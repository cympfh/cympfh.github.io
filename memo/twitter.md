% Twitter API
% API

## API

- `.user`
    - `.user.name` ← ニックネーム
    - `.user.screen_name`
- `.id_str` ← とても長い
- `.text`
- `.media` ← 配列
    - `.media[].media_url`
    - `.media[].media_url_https`


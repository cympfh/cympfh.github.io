#!/bin/bash

if [ $# -ne 1 ]; then
  cat << HELP
Usage: thumb <image>

resize the image to 800x800
output image is *.thumb.jpg when input is *.jpg
HELP
  exit 0
fi

EXT=${1##*.}
BASE=${1%.*}

convert -resize 800x800 "$1" "${BASE}.thumb.${EXT}"

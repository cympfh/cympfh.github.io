# 数列 - 乗法セグメントツリー

## 概要

`i64` 上の乗法に関するセグメントツリーの定義を与える.
ここで `i64` は既にモノイドとしてあるので, 具体的な実装は必要ない.

### Note

整数型の区間積なんてすぐオーバーフローするので, これをそのままで使うことはあまり無いと思われる.

## 実装

@[rust](procon-rs/src/sequence/tree/segment_tree_prod.rs)

# paper/

## `src/YYYY/*.md`

```markdown
% {title}
% {url}
% {short summary}
% {tags}

...markdown...
```

## make pages

generates `YYYY/*.html`
using `template/page.hbs`.

## make cache (for index)

generates summaries `cache/YYYY/*.html`

## make index

generates `index.html`
